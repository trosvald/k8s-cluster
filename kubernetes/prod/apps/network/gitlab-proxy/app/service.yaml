---
apiVersion: v1
kind: Service
metadata:
  name: ${APP}
spec:
  externalName: mattermost.monosense.io
  ports:
    - name: http
      port: 80
      protocol: TCP
      targetPort: 80
  type: ExternalName
