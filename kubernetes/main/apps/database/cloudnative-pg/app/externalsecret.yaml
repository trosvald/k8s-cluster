---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/external-secrets.io/externalsecret_v1beta1.json
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: cloudnative-pg
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: onepassword-connect
  target:
    name: cloudnative-pg-secret
    template:
      engineVersion: v2
      metadata:
        labels:
          cnpg.io/reload: "true"
  data:
    - secretKey: username
      remoteRef:
        key: cloudnative-pg
        property: cnpg_super_user
    - secretKey: password
      remoteRef:
        key: cloudnative-pg
        property: cnpg_super_pass
    - secretKey: aws-access-key-id
      remoteRef:
        key: cloudnative-pg
        property: cnpg_s3_access_key
    - secretKey: aws-secret-access-key
      remoteRef:
        key: cloudnative-pg
        property: cnpg_s3_secret_key
